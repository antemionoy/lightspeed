import type { EcwidProduct, EcwidGalleryImage, EcwidMediaImage, EcwidMedia } from '@shared/api/ecwid/types';

export type Product = EcwidProduct;
export type GalleryImage = EcwidGalleryImage;
export type MediaImage = EcwidMediaImage;
export type Media = EcwidMedia;
